Run with `npx ts-node --project ./tsconfig.json -r tsconfig-paths/register src/scripts/seed.ts`

The `-r tsconfig-paths/register` part is to make import aliases (@/*) work. See tsconfig-path package doc for more.
